<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginTop="8dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="8dp"
    android:background="@android:color/transparent"
    app:cardBackgroundColor="@android:color/transparent"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    app:cardPreventCornerOverlap="true"
    app:cardUseCompatPadding="true">

    <RelativeLayout
        android:id="@+id/itemLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:padding="12dp">

        <ImageView
            android:id="@+id/imageViewBackground"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:background="@android:color/transparent"
            android:scaleType="fitXY"
            app:srcCompat="@drawable/rectangle_124" />

        <!-- Status Icon di kanan atas -->
        <ImageView
            android:id="@+id/imageViewStatus"
            android:layout_width="wrap_content"
            android:layout_height="31dp"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="24dp"
            android:layout_marginEnd="8dp"
            android:background="@android:color/transparent"
            app:srcCompat="@drawable/menunggusye" />

        <!-- Nama Siswa -->
        <TextView
            android:id="@+id/textViewNamaSiswa"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="12dp"
            android:text="Nama Siswa -"
            android:textColor="#000000"
            android:textSize="11sp"
            android:textStyle="bold" />

        <!-- Kelas/Jurusan -->
        <TextView
            android:id="@+id/textViewKelas"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginTop="12dp"
            android:layout_toEndOf="@id/textViewNamaSiswa"
            android:text="12 Rekayasa Perangkat Lunak 2"
            android:textColor="#000000"
            android:textSize="11sp" />

        <!-- Label Mata Pelajaran -->
        <TextView
            android:id="@+id/labelMataPelajaran"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/textViewNamaSiswa"
            android:layout_marginStart="12dp"
            android:layout_marginTop="8dp"
            android:text="Mata Pelajaran :"
            android:textColor="#000000"
            android:textSize="11sp"
            android:textStyle="bold" />

        <!-- Mata Pelajaran -->
        <TextView
            android:id="@+id/textViewMataPelajaran"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/textViewNamaSiswa"
            android:layout_marginStart="8dp"
            android:layout_marginTop="8dp"
            android:layout_toEndOf="@id/labelMataPelajaran"
            android:text="Bahasa Indonesia"
            android:textColor="#000000"
            android:textSize="11sp" />

        <!-- Icon tambahan untuk disetujui -->
        <ImageView
            android:id="@+id/imageViewCheckmark"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="28dp"
            android:layout_marginEnd="48dp"
            android:background="@android:color/transparent"
            android:visibility="gone"
            app:srcCompat="@drawable/check_circle2" />

        <!-- Icon tambahan untuk ditolak -->
        <ImageView
            android:id="@+id/imageViewCancel"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="28dp"
            android:layout_marginEnd="48dp"
            android:background="@android:color/transparent"
            android:visibility="gone"
            app:srcCompat="@drawable/cancel2" />

    </RelativeLayout>

</androidx.cardview.widget.CardView>