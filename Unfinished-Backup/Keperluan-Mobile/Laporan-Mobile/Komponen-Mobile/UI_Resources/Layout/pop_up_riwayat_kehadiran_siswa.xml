<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_gravity="center"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <!-- Judul di atas center horizontal -->
    <TextView
        android:id="@+id/tv_judul_popup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Absensi Siswa"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="40dp"/>

    <!-- RecyclerView di bawah judul dengan tinggi tetap -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_riwayat_siswa"
        android:layout_width="350dp"
        android:layout_height="600dp"
        android:scrollbars="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.491"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_judul_popup"
        app:layout_constraintVertical_bias="0.673"
        tools:itemCount="10"
        tools:listitem="@layout/bar_riwayat_siswa_wakel" />

</androidx.constraintlayout.widget.ConstraintLayout>